#include <stdio.h>

int main(void)
{
    float radius = 2.57;
    float pi = 3.1416;
    float circumference = 2 * pi * radius;
    printf("The circumference of a circle with radius %f is %f", radius, circumference);

    return 0;
}